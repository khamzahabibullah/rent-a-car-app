<div class="container">
    <h1><%= @car.name %></h1>
  <div class="cards">
  </div>
</div>

<%= link_to "book", car_path(@car.id), class: 'btn btn-primary'%>
<%= link_to 'Back', cars_path, class: 'btn btn-secondary' %>
<%= link_to 'Delete',
            method: :delete,
            class: 'btn btn-danger',
            data: { confirm: 'Are you sure ?'} %>
