<%= @car.name %>



<%= link_to "book", car_path(@car.id) %>
<%= link_to 'Back', cars_path, class: 'btn btn-secondary' %>
<%= link_to 'Delete',
            car_path(@car),
            method: :delete,
            class: 'btn btn-danger',
            data: { confirm: 'Are you sure ?'} %>
